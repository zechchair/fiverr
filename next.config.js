/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  env: {
		color: "blue",
		secondColor: "green",

	},
	// typescript: {
	// 	ignoreBuildErrors: true,
	//   },
}

module.exports = nextConfig
